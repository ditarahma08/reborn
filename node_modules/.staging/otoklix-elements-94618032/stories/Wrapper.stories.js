import React from 'react';
import { MobileWrapper as MobileWrapperComponent } from '../src/index';

const size = ['xs', 'sm', 'md', 'lg', 'xl', 'xxl'];

export default {
  title: 'Components/Wrapper',
  component: MobileWrapperComponent,
  argTypes: {
    size: {
      control: {
        type: 'radio',
        options: size,
      },
    },
    cssModule: {
      table: {
        disable: true,
      },
    },
  },
};

export const MobileWrapper = (args) => (
  <MobileWrapperComponent
    {...args}
    className="shadow-lg p-3 border border-dark"
  >
    <h1>Hello World</h1>
  </MobileWrapperComponent>
);
MobileWrapper.args = { size: 'xs' };
