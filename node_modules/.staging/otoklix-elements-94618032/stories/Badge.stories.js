import React from 'react';
import { Badge as BadgeComponent, Col, Row } from '../src/index';
import { BgColors } from './constants';

export default {
  title: 'Components/Badge',
  component: 'BadgeComponent',
  argTypes: {
    cssModule: {
      table: {
        disable: false,
      },
    },
    color: {
      control: {
        type: 'radio',
        options: BgColors,
      },
    },
  },
};

export const Playground = (args) => (
  <div className="story-wrapper">
    <Row>
      <Col>
        <BadgeComponent {...args} />
      </Col>
    </Row>
  </div>
);
Playground.args = { pill: false, children: 'Otoklix' };

export const Variants = () => (
  <div className="story-wrapper">
    <Row>
      {BgColors.map((variant) => (
        <Col>
          <BadgeComponent color={variant}>{variant}</BadgeComponent>
        </Col>
      ))}
    </Row>
  </div>
);
Variants.parameters = { controls: { disable: true } };
